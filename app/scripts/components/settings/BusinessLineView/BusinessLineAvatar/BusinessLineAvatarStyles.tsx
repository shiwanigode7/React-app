import { makeStyles } from "@material-ui/core";

export const BusinessLinesAvatarStyles = makeStyles(()=>({

    rootDiv: {
        display: "inline-flex",
        flexWrap: "wrap"
    },

    avatar: {
        margin: "2px"
    },

    tooltip: {
        fontSize: "10px"
    },
}));