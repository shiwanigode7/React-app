import { makeStyles } from "@material-ui/core";

export const LoadingIconStyles = makeStyles((theme: any) => ({
    loadingIcon: {
        color: "black",
        position: "absolute",
        left: "50vw",
        top: "50vh",
        zIndex: 1
    }
}));