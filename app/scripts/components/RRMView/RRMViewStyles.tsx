import { makeStyles } from "@material-ui/core";
import { COLOR_GREY_3 } from "../../constant/Colors";

export const RRMViewStyles = makeStyles({
    rrmView: {
        background: COLOR_GREY_3
    }
});
