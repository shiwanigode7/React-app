import { makeStyles } from "@material-ui/core";
import { COLOR_GREY_3 } from "../../constant/Colors";

export const ReleaseViewStyle = makeStyles({
    releaseView: {
        background: COLOR_GREY_3,
        minWidth: "100 %"
    },
    releaseContent: {
        width: "100%"
    }
});
